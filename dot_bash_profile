export EDITOR='nvim'
export MANPAGER='nvim +Man!'

command_exists() {
    command -v "$1" >/dev/null
}

if [[ -f ~/.bashrc ]]; then
    source ~/.bashrc
fi

if uwsm check may-start && command_exists niri; then
    exec uwsm start niri.desktop
fi
